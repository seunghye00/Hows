<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Product">

	<select id="getProducts" resultType="com.hows.product.dto.ProductDTO">
		SELECT * 
        FROM PRODUCT ORDER BY product_seq
	</select>

	<select id="getProductByCategory" parameterType="String" resultType="com.hows.product.dto.ProductDTO">
        SELECT * 
        FROM PRODUCT
        WHERE product_category_code = #{product_category_code}
    </select>
    
    <select id="getProductDetaile" parameterType="String" resultType="com.hows.product.dto.ProductDTO">
    	SELECT * 
		FROM PRODUCT
		WHERE product_seq = #{product_seq}
    </select>
    
    <insert id="insert">
    	<selectKey keyProperty="product_seq" resultType="int" order="AFTER">
        	select product_seq.currval from dual
   	 	</selectKey>
    	insert into product values (product_seq.nextval, ' ', #{product_title}, #{product_contents}, #{price}, #{product_category_code})
    </insert>
    
    <update id="updateThumbNail">
    	update product set product_thumbnail = #{url} where product_seq = #{seq}
    </update>
    
    <!-- 리뷰등록 -->
	<insert id="insertReview" parameterType="com.hows.product.dto.ReviewDTO">
	    INSERT INTO review (review_seq, rating, review_contents, review_date, product_seq, member_id)
	    VALUES (review_seq.nextval, #{rating}, #{review_contents}, SYSDATE, #{product_seq}, #{member_id})
	</insert>


</mapper>